#!/usr/bin/env bash
# Wrapper to run IMM predictor as a module so relative imports work
export PYTHONPATH="/home/victor-tipkemper/hackathon/orion/src/orion_flight:${PYTHONPATH:-}"
exec python3 -m orion_flight.predictors.imm.imm_predictor_node "$@"
